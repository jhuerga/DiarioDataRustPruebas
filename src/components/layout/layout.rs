use yew::prelude::*;

#[function_component(Layout)]
pub fn layout() -> Html {
    html! {
        <div class="layout-container">
            { "Layout base (puedes personalizarlo cuando quieras)" }
        </div>
    }
}
